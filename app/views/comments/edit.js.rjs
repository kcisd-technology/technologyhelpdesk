page["comment_body_#{@comment.id}"].update(
  render(:partial => 'inline_form', :object => @comment)
);
page << <<HEREDOC
$('cancel_edit_#{@comment.id}').observe('click', cancelCommentEdit);
$('comment_body_#{@comment.id}').down('form').observe('submit', submitCommentChanges);
HEREDOC
