%h2= "Editing #{@user.login}"
-form_for @user do |f|
  =f.error_messages
  %p
    =f.label :email
    %br/
    = f.text_field :email
  =render :partial => 'roles', :locals => {:f => f}
  %p
    =f.submit "Update"
=link_to 'Show', @user
|
=link_to 'Back', users_path